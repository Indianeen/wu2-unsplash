---
layout: base.njk
---

<main>
    <div class="container">
                <div class="box-1"> 
            {{item || dump(1)}}
        </div>
                <div class="box-2"> 
            {{item || dump(2)}}
        </div>
                <div class="box-3"> 
            {{item || dump(3)}}
        </div>
                <div class="box-4"> 
            {{item || dump(4)}}
        </div>
                <div class="box-5"> 
            {{item || dump(5)}}
        </div>
    </div>
</main>